{
  "name": "youtube-rtc",
  "version": "1.0.0",
  "description": "Youtube Control Usign WebRTC",
  "main": "index.js",
  "presets": [
    "es2015"
  ],
  "scripts": {
		"build-babel": "./node_modules/.bin/babel src -d dist",
    "dev-babel"  : "./node_modules/.bin/babel --watch src -d dist",
    "update-page": "git checkout gh-pages;git pull origin master;npm run build;git add .;git commit -m 'Updating';git push origin gh-pages"
  },
  "author": "Augusto Monteiro",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-polyfill": "^6.9.0",
    "stylus": "^0.54.5"
  }
}
